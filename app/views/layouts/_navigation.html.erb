<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Facebug', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Create a post', new_post_path %></li>
        <li><%= link_to 'All users', users_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.friend_requests_count.zero? %>
          <li><%= link_to 'Friend requests', friend_requests_user_path(current_user) %></li>
        <% else %>
          <li><%= link_to "Friend requests <span class='badge'>#{current_user.friend_requests_count}</span>".html_safe, friend_requests_user_path(current_user) %> </li>
        <% end %>
        <li><%= link_to 'Friends', friends_user_path(current_user) %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
            <li><%= link_to 'Profile', user_path(current_user) %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
